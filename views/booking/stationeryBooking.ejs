<% layout("/layouts/boilerplate.ejs") %>

<div class="container my-4">
    <h4 class="mb-3">Booked Stationery Items</h4>
    <table class="table table-bordered table-striped">
      <thead class="table-dark">
        <tr>
          <th>User</th>
          <th>Item</th>
          <th>Quantity</th>
        </tr>
      </thead>
      <tbody>
        <% for(item of boockedItem) { %>
          <tr>
            <td><%= item.userId.username %></td>
            <td><%= item.bookingId.title %></td>
            <td><%= item.quantity %></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
  
  